# Phân tích offline

Các file trong thư mục này được sinh lại từ `ddos_sample.csv` và `mailbomb_sample.csv` của gói lab bằng `build_lab3.py` (Python) để đối chiếu với output PowerShell `Import-Csv | Group-Object` của đề. Dữ liệu dùng dải TEST-NET (RFC 5737) và domain `.invalid` (RFC 2606), không gửi email, không tạo lưu lượng.
